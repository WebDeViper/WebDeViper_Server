const express = require('express');
const router = express.Router();
const controller = require('../controller/ctrChat.js');
//방정보
// router.get('/rooms', controller.getAllRooms);

router.module.exports = router;
